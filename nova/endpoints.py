class webdirs():
    connection_check = "/connectivity_check"
    players_online = "/players/online"
    basic_network_stats = "/stats/basic"
    extended_network_stats = "/stats/extended"
    player_stats = "/stats/player/{}"

class visage_webdirs():
    full = "/full/{}"

class mojang_webdirs():
    player_profile = "/users/profiles/minecraft/{}" #Used to find player uuid from ign.